# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!supabase/functions/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP session logs/snapshots
.playwright-mcp/
test-results/

# Loose screenshots at repo root (test/debug artifacts)
/admin-*.png
/parceiro-*.png
/parceiros-*.png
/qr-*.png

# Local document fixtures (private / personal data)
.tmp/test-docs/

# Local ephemeral scripts and analysis artifacts (.tmp/)
# Mantemos `.tmp/pg-snapshot-validate/` versionado por ser validador da
# migration `add_layout_to_bot_flow_steps` (referenciado em
# .kiro/specs/flow-diagram-view/migration-15-3-validation.md).
.tmp/*
!.tmp/pg-snapshot-validate/

# Lovable workspace metadata (gerado automaticamente)
.lovable/

# Kiro settings — contém tokens em texto puro (NUNCA commitar)
.kiro/settings/mcp.json

# Cursor MCP local (caminhos absolutos da máquina; usar mcp.json.example)
.cursor/mcp.json

# TypeScript incremental build cache
*.tsbuildinfo

# Supabase CLI temp/cache
supabase/.temp/

# Artefatos locais de deploy de edge (payload/resposta)
.deploy-parts/
.tmp-deploy-*.json
.tmp-mcp-*.json

# TestSprite local artifacts
testsprite_tests/


testsprite_tests/tmp/config.json
