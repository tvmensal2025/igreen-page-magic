---
inclusion: always
name: armadilhas
description: Sintoma → correção canônica. Leia antes de consertar.
---

# Armadilhas — erros típicos de modelo fraco

Sintoma → correção canônica. Se for fazer o lado esquerdo, pare.

1. **Evolution `needs_reconnect` = Zap offline** → Health Whapi `AUTH`. Regra Whapi primário.
2. **Misturar 3 “em análise”** → `src/lib/crmVsLeadAnalysis.ts`.
3. **Filtrar só `status=pending`** → use `conversation_step` + `portal_submitted_at` + `do_not_contact`.
4. **Rodízio por cidade/texto** → `deterministic-campaign-resolver.ts` + UUID `source_campaign_id`.
5. **Chamar com `whatsapp_profile`** → `safeFirstNameForAddress` (`customer-display-name.ts`).
6. **`display_name || name` do consultor** → `resolvePublicConsultantLabel`.
7. **Portal 1 / URL sync errada** → `dispatchPortalWorker` = Portal 2; sync = `igreen_sync_worker_url`.
8. **Apagar migration/toggle/flag** → arrumar; não deletar. Kill: `isBotGloballyEnabled`.
9. **Ligar envio em massa novo** → só sob pedido explícito + cadeados; E2E `dryRun`.
10. **Quiet hours em agenda humana** → `send-scheduled-messages` **sem** quiet; quiet = bot (`quiet-hours.ts`).
11. **Importar `_shared/vendedora/`** → produção `cerebro/resposta-hook.ts`; simulador `_shared/fluxo-b-ia/`.
12. **Tabela `messages`** → chat = `conversations`.
13. **`webhook_message_dedupe`** → canônico `webhook_message_dedup` (`_shared/bot/dedupe.ts`).
14. **Label “DNC” na UI** → “bloqueado” / “nunca mais contatar” (`isNuncaMaisContatar`).
15. **Protocolo na msg WA** → `2026-####` só banco/admin.
16. **Club com HMAC/fluxo do Portal** → workers e colunas separados (`club_*` ≠ `portal2_*`).
17. **Pós-venda “usa `bot_global`” / só D120 / “via Evolution”** → marcos **D30…D210** + **retentativa**; canal Whapi primeiro; toggle `pos_venda_auto_messages` + `pos_venda_manual` (**não** exige `bot_global`).
18. **Cross-sell em massa ou chamar `avaliarCrossSell` sem consumidor** → card/manual (`#cross-sell`); Cérebro em sombra (`CROSS_SELL_SHADOW`); só avaliar com consumidor elegível e só ligar massa com pedido explícito.
19. **Assumir que ads / cérebro-mg / minio só carregam via `#nome`** → frontmatter real é `inclusion: auto` (CI `check:agent-docs` confere).
20. **Seguir spec Evolution-first em `.kiro/specs`** → leia `.kiro/specs/STATUS.md`; regra dura Whapi vence.
21. **Números “de memória”** → `#evidencia-prod` / `mapa-dominios.json` (snapshot auditado).
22. **Fatura via `extract-receipt`** → fatura = `POST /extractor/extract` (ver PORTAL-OFICIAL).
23. **CORS `*` em edge nova sensível** → preferir `buildCors(req)`.
24. **Cron sem `assertCronAuth`** → sempre gate; preferir 200 `{ skipped }` a 5xx barulhento.
25. **Assumir que o V3 já decide o turno** → `flow_engine_v3` ainda é sombra/canário conforme rollout; preserve a ordem real de roteamento documentada em `#wa-webhook`.
26. **Somar action_types de conversa Meta** (`started`+`first_reply`+`total_connection`) → triplica conversas e CPL cai ~3×; Cérebro sobe budget à toa. Use `pickMetaConversations` / `pickMetaLeads` em `_shared/meta-insight-actions.ts` (prioridade, nunca soma).
27. **Motor global ON = consultor ON** → falso. Cadeado 2: `consultant_automation_prefs` (A/B/C, pós-venda, lembretes). Sem row / pack OFF = skip só daquele `consultant_id`. UI: modal + “Minhas automações”. Helper: `consultant-automation-prefs.ts` / `src/lib/consultantAutomationPrefs.ts`.
28. **“Velip sem crédito → sistema pausa sozinho”** → API v2 **não expõe saldo**; **não há gate** de crédito no código. Consultar **painel Velip**; recarregar lá. Playbook: `#erros-operacionais`.
29. **`sms_sent` = chegou no celular** → falso. Aceito Velip ≠ `DELIVRD`. Rajada = operadora engole. Ver `voice_sms_log.delivery_status` (`UNDELIV`/`Blocked text#270`/null).
30. **IK/EK/BK/CK ou UNDELIV×2 e “tenta de novo”** → canal morto / DNC; `checkPhoneDeadForChannel`. Não gastar saldo.
31. **Easy Panel “o worker”** → são **3**: Portal2 ≠ Club ≠ Sync (`d9v63q`, nunca typo `d9v83a`). Health de cada URL.
32. **Plataforma não abre = Zap/Evolution** → checklist `#erros-operacionais` §4 (cache → JWT → CORS → Whapi AUTH → workers → cron auth).
33. **Qualquer falha operacional sem playbook** → comece por `#erros-operacionais` (Velip/IA/OCR/EasyPanel/Supabase/caps).
34. **Cliente recebendo A/B/C como lead** → trava `isClienteProibidoCadenciaABC` + DB `tg_lead_cadence_block_cliente` + `cadence_ensure_state_from_customer` (sync não cria GREETED). Cliente = carteira/aprovado/`pos_venda_stage`/andamento ativo → só pós-venda + agenda. Os N do KPI “Total de cadastros” são carteira sync, não pizza A/B/C.
35. **“Falha operacional e ninguém avisa”** → cron `super-admin-alerts` (Whapi → `super_admin_phone`) + MinIO via `_shared/superadmin-alert.ts`. Não depende de Evolution. Dedup `infra_metrics` (`ops_alert` / `minio_alert`). Playbook: `#erros-operacionais` §0b.
36. **`cerebro_ativo=on` = Cérebro conduz o Grupo A inteiro** → falso. Grupo A / cadastro determinístico manda nos passos (OCR, doc, portal). Cérebro só: dúvida livre no meio, fora do cadastro, ou cliente carteira. Gates: `fluxo-a-bypass` + `classifyCadastroInput` em `whapi-webhook`. Ver `#cerebro-fluxo-b`.
37. **`quota_min_interval_not_elapsed` = “40 pessoas falharam”** → falso. Contar `COUNT(DISTINCT customer_id)`, não linhas do log. Era bug: `cadence-tick` tratava intervalo anti-ban como `failed`+30min. Correto: `awaitOutboundSendQuota` (Whapi bypassa → fila `claim_whapi_send_slot`; Evolution espera slot / softDefer em segundos).
38. **`identity_missing:consultor_phone` ≠ `identity_missing:consultor`** — nome sumiu após 2026-07-25 (presentation label). Phone: amarrar `wa.me` ao **canal real** (`channelKind=whapi` → `settings.whapi_connected_phone`; Evolution só se status saudável). Nunca usar `connected_phone` de `needs_reconnect` (Silvia: WA Whapi 5534 + SMS wa.me 5514).
39. **Lead responde COLD_1/B e não entra no Grupo A** → trigger `cadence_on_inbound_message` NÃO pode forçar `AI_QUALIFYING` em cima de COLD_/SMS_/CALL_/RECALL_ (migration `20260728160000_restore_cadence_inbound_preserve_bc`). Sintoma: `cadence_action_log.detail.prev_stage=AI_QUALIFYING` + `from_bc=false` logo após COLD_1. Também: `aguardando_avaliacao_atendimento` ≠ funil A (`isActiveGroupAConversation`). Protocolo vazio no start attendance: nunca passar `protocolo:""` ao `resolveConsultantMessage` antes do assign — deixa `{{protocolo}}` p/ `sendWelcomeHeader`.
40. **Lead Grupo A (Sofia UUID) “cai no Cérebro / outro fluxo”** → `_emCadastro` só olhava `CADASTRO_STEPS` (keys legadas). Step `a1_ask_name` gravado como UUID → `!_emCadastro` → Cérebro assume (Leandro Severiano 2026-07-28). Fix: OR `isActiveConversationalFunnelStep(step)` (UUID / `aN_` / `flow:`) em whapi+evolution. Custom start_attendance sem protocolo: limpar linha `Protocolo:**` em `attendance-flow`. Protocolo `IGR-IGR-####` = iniciais vazias no assign — buscar nome do consultor e nunca usar meio `IGR`.
41. **Abertura falando “Gestor” / “sua consultora” no lugar do nome** → legado `buildWelcomeHeaderProtocol` tinha `, *Gestor*` + fallback `*consultora*`. **Regressão 2026-07-29 (Igor):** fix cobria `attendance`/`renderVars`, mas o **A1 do flow** usa `_shared/bot/conversational-templates.renderTemplate` (sem scrub) + 1º “Oi” com `step=null` no Grupo A caía no Cérebro (`!_emCadastro`). Scrub antigo NÃO casava `Aqui é a sua *consultora* da`. Fix definitivo: (1) scrub cobre possessivo fora do bold; (2) `renderTemplate` do flow sanitiza papel + scrub; (3) webhook usa `resolveConsultantPresentationLabel` (nunca possessivo); (4) boot `welcome/null` no variant A = `_emCadastro` (fluxo-a-bypass); (5) scrub no `applyTurnResult` + Cérebro antes do envio + log em `conversations`. Sem nome humano → “Aqui é o atendimento da *iGreen*”.
41. **Subconta sync “não puxa telefone” / cliente fica `sem_celular_*`** → Conta principal mascara `celular` da rede; unique `(consultant_id, igreen_code)` trava 1 linha. **UPDATE por igreen_code** (não upsert só por phone); enrich **sem** filtrar `igreen_account_id`; `enrich_only` reprocessa placeholder pelo `registered_by`. Ver `#igreen-sync-oficial`.
42. **Cliente carteira recebe msg de lead novo** → sync grava colisão `5511…_igreenCode` e deixa lead sombra no número limpo; webhook buscava só exact → Grupo A no lead. Corrigir: `_shared/inbound-customer-resolve.ts` (prioriza `igreen_sync`), absorver sombra no sync, não notificar carteira como novo lead.
43. **Pós-venda manda 4 msgs iguais no mesmo Zap** → NÃO é falha do UNIQUE `(customer_id, stage_key)`. São **2 rows** sync (limpo + `5511…_igreenCode`); `toJid` antigo colava o código no JID e sibling por `remote_jid` exact falhava. Fix: pula colisão (`_` **ou** dígitos >13) + sibling por **phone base** + `normalizePhone` corta `_codigo` + spacing 25d (`skipped_spacing`); invalidar sombra (`pos_venda_invalid`). Ver `#pos-venda`.
44. **OCR bom → pedir SIM/NÃO/EDITAR** → **não** é mais o happy path. Conta/doc com confiança ok avançam sozinhos (`advanceSofiaToDocumentAfterBill` / `advanceGenericToDocumentAfterBill` / `autoAdvanceAfterDocOcr`); `bill_data_confirmed_at` / `doc_data_confirmed_at` com `*_confirmation_by=auto_ocr`. Baixa confiança → retry curto só daquele anexo (`OCR_RETRY_*_SHORT`). Steps `confirmando_dados_*` ficam só para legado/edição explícita. Paridade Whapi+Evolution.
45. **`Date.now()` / bucket em `idempotencyKey` = bypass do teto `outbound_effects`** → chave nova a cada tick/janela anula `failed_retryable<5`. Sintoma: mesmo lead com dezenas de envios (ex.: A_NUDGE×87). FAÇA: chave estável (`journey:…`, `bulk:${targetId}`, `scheduled:${msgId}`, `welcome:${customerId}`); cooldown de produto via coluna (`nudge_sent_at` também no **fail**). Nunca `finishOutboundEffect(...,"failed")` — CHECK só aceita `failed_retryable`/`failed_final`. ACK reopen: se não ler `attempt_count`, assume **teto** (fail-closed), não 0. SMS/voz do daily-reheat também passam por `reserveOutboundEffect`.
46. **Envio a lead sem `assertCanContact`** → `finalize-capture`, `recover-stuck-otp`, `worker-callback`, **`voice-dialer-webhook` SMS NA**, **`whapi-proxy`/`evolution-proxy` chat manual**, **`platform-sales-dispatch`** precisam do gate DNC. Portal/OTP: use **só** `assertCanContact` (não `assertBotOutboundAllowed`). Pós-venda: `isPausedByPhone` já cobre DNC. Teste: `src/test/outbound-dnc-coverage.test.ts`.
47. **`recon-igreen-*` sem auth** → `worker` / `seed` / `endpoints`: auth híbrida `assertCronAuth` **ou** admin JWT (`resolveCaller.isAdmin`). `config.toml` com `verify_jwt=false`. Sem isso, anon/JWT comum podia consumir fila, ler senha portal + IA.
48. **Evolution 403/401 → auto-QR / `recreateInstance(auto_fatal)`** → **PROIBIDO**. Queima número antigo (anos). Correto: `register_fatal_disconnect` (14d) + UI `fatalLocked` + alerta super-admin. Recreate só `manual_admin` após `admin_clear_fatal_lock`. Settings create: `rejectCall`/`groupsIgnore` on; sem `syncFullHistory`/`alwaysOnline`. Sem Evomi no Evolution. Ver `docs/ANTI_BAN_AUDIT.md` + `handlers/connection.ts`.
49. **Conectar Evolution = logout+delete+nome fresco** → **PROIBIDO** (ban sem spam; Abel 2026-07-28). Correto: `getFixedInstanceName` + `create` só se `missing` + `connect` na mesma; logout/delete só em Reset/trocar chip. UI: “Reconectar (mesma sessão)”, cooldown QR 30s, Reset = último recurso. `recreateInstance(manual_admin)` também reusa **nome fixo** (`baseNameOf`) — nunca sufixo timestamp (gera zumbis multi-QR).
50. **IDOR em envio/finalize** → `finalize-capture` e **`manual-step-send`**: JWT sozinho não basta. Exigir posse `customer.consultant_id === consultantId` (ou admin/`x-service-secret`). `evolution-proxy`: instância no path deve ser do consultor (`whatsapp_instances.consultant_id`).
52. **Alerta “falhas de cadência” com Whapi AUTH** → quase nunca é chip caído. Causas reais: (a) `origin_channel=evolution` + `origin_instance_name=whapi-*` (404 Evolution); (b) telefone lixo / país errado martelando `whapi_send_returned_false`; (c) consultor sem `whatsapp_instances` whapi* no compartilhamento intencional (Sirlene). Correto: normalizar origem; `isPlausibleBrWhatsAppPhone` + permanent; gravar `whapi-sirlene` (etc.) no consultor que compartilha o chip.
51. **`Date.now` em ctid Velip** → `platform-sales-dispatch` usa `ps_{target}_{day}` / `ps_call_{target}_{day}`; `voice-sms-send` usa consultor+fone+hash(texto)+dia. Sem timestamp na chave.
53. **“Parceiro tem keyword + QR, lead converteu, mas não entrou pra ele”** → NÃO é keyword errada nem `is_active=false`. A frase padrão do QR (`buildDefaultQrPhrase`) continha **“quero saber mais”**, que é âncora de `META_CTWA_OPENING_PHRASES`. Logo `matchesMetaCtwaPhrase(texto)=true` → `blockKeywordForMetaLead` → bloco de atribuição de parceiro **pulado inteiro** nos dois webhooks. Determinístico, 100% dos leads com frase padrão. Correções: (a) frase padrão CTWA-safe nos **3** geradores (`_shared/qr-phrase.ts`, `src/components/admin/parceiros/qrPhrase.ts`, cópia local do `qr-redirect`); (b) gate canônico `evaluatePartnerKeywordGate` (`_shared/partner-attribution-gate.ts`) — só sinal **FORTE persistido** (`source_campaign_id`/`source_ad_id`/`source_ctwa_clid`/`ctwa_clid`/`lead_source~meta`) bloqueia; frase que “parece CTWA” é sinal **fraco** e nunca veta keyword exata/`#R`. Testes que travam: `qr-phrase-ctwa-safety_test.ts` + `partner-attribution-gate_test.ts`.
54. **Buscar `referral_partners` só pelo consultor do canal** → no `whapi-webhook` era `consultant_id = superadmin_consultant_id` e no `evolution-webhook` `instanceData.consultant_id`: o **mesmo** parceiro funcionava num canal e não no outro. Use `resolvePartnerScopeConsultantIds(customer.consultant_id, superAdmin|instancia)` + `.in("consultant_id", scope)` e `orderPartnersByScope` (dono do lead vence empate; outro tenant é descartado). Banner do consultor também segue o **dono do lead**.
55. **Guard de AD ID zerando parceiro de QR/keyword** → `enforce_customer_meta_ad_campaign_guard` exigia o parceiro pertencer à pool da campanha do `source_ad_id`; parceiro indicador **nunca** está em pool, então ganhar `source_ad_id` depois fazia `referral_partner_id := NULL` em silêncio (ou 23514 que o webhook só logava em `console.warn`). Migration `20260805120000`: se `referral_keyword_matched` é comprovadamente do parceiro (keyword dele, keyword de spot dele, ou `#R{short_code}` dele), o guard **preserva**. Rodízio nunca grava `referral_keyword_matched` → rodízio intacto. Falha de escrita agora vira `markManualReview('partner_attribution_write_failed')` + `campaign_match_log method='keyword_write_failed'`.
56. **“Fechamento” do parceiro só com `pos_venda_stage='Aprovado'`** → cliente com `is_converted`, `status='approved'`/`registered_igreen`, `andamento_igreen='ativo'` ou já na carteira **não contava** — nem no portal `/p/{token}` nem no analytics do admin — e ainda saía de `cycle_leads` (que exclui convertido). O parceiro ficava sem lead na pizza **e** sem fechamento. Use `public.customer_is_closed_deal(...)` (migration `20260805121000`, espelho de `cliente-cadence-guard`) nos **dois**. Novo `stats.em_analise` = `portal_submitted_at` sem aprovação. Contagens ignoram `bot_paused_reason='absorbed_wallet_duplicate'` (sombra da carteira) para não contar 2×.
57. **Absorver lead sombra da carteira e perder o parceiro** → `absorbLeadShadowsForWalletPhones` marcava `do_not_contact=true` na sombra sem migrar `referral_partner_id` para a linha de carteira; portal e `notifyPartnerNewLead` filtram DNC → crédito do parceiro evaporava justo quando o cadastro dava certo. Agora `carryPartnerAttributionToWalletRow` copia `referral_partner_id`/`referral_keyword_matched`/`referral_detected_at` com CAS (`.is("referral_partner_id", null)`), antes do DNC.
58. **Keyword de parceiro que é palavra comum (“Zap”)** → o parceiro José cadastrou **`Zap`**. “Zap” é como o brasileiro chama WhatsApp: qualquer lead que escrevesse “me chama no zap” casaria com ele — a keyword não identificava, **sorteava**. Dois defeitos somados: (a) `zap`/`whatsapp`/`wpp` não estavam na blocklist; (b) `GENERIC_KEYWORD_BLOCKLIST`/`isGenericKeyword` existiam **só no front** (`src/components/admin/parceiros/qrPhrase.ts`), zero ocorrências em `supabase/` — keyword vinda de import/SQL/cadastro antigo passava direto. Corrigido: régua canônica agora em **`_shared/keyword-matcher.ts`** (`isGenericKeyword` + `KEYWORD_MIN_LENGTH=3` + `findGenericKeywords`), e **`matchKeyword` descarta keyword genérica** — trava que não depende da UI ter validado. Comparação é da keyword **inteira**, então `posto` é bloqueado mas `posto shell br 101` continua valendo. Front é espelho, travado por `__tests__/keywordBlocklistParity.test.ts`. UI (`PartnerDashboard`, `PartnerQuickCard`) não conta keyword genérica como “configurado” e mostra `ignorada: <riscada>`.
59. **Banner FÍSICO de parceiro em loja não é rodízio nem Meta** → cenário canônico do José: vários parceiros do **mesmo** consultor, cada um com banner num ponto, todos caindo no **mesmo número de WhatsApp**. Aqui o `short_code` da URL é determinístico, mas é **descartado no redirect**: do `wa.me` em diante o único carregador da atribuição é o **texto** da mensagem (`page_events.qr_scan` não tem telefone — o WhatsApp só abre depois do scan). Consequências a lembrar: `DETECTION_WINDOW=3` faz cliente que já conversou antes nunca ser atribuído; lead que apaga o texto pré-preenchido perde a indicação; e sem `#R{short_code}` (desligado em 2026-08-03) não existe desempate determinístico entre parceiros do mesmo número. **Não** aplicar raciocínio de rodízio/Meta nesse fluxo.
60. **Fechamento do parceiro olhando UMA linha de `customers`** → o cliente pode **começar a conversa num celular e cadastrar em outro** (caso José 2026-08-05: falou do `…99195340`, cadastrou no `…99356886` para ficar no mesmo app). O lead com `referral_partner_id` para em `cadastro_concluido` e o “Validado/aprovado” chega numa linha `igreen_sync` **sem** parceiro — mesmo CPF, mesmo e-mail, mesmo `igreen_code`. `customer_is_closed_deal` avalia linha a linha, então o parceiro via **0 fechamentos / 1 em análise** numa venda real. `carryPartnerAttributionToWalletRow` **não** cobre: ela casa só por telefone. Use `public.partner_lead_is_closed(...)` (migration `20260805210000`), que aceita a **linha irmã** fechada do mesmo consultor por `igreen_code` (mesma venda) ou por `cpf` quando o lead tem `portal_submitted_at`. Nunca cruza consultor e segue ignorando `absorbed_wallet_duplicate` — sem dupla contagem.
61. **Prenome solto como chave do parceiro** → o cadastro salvava `rafael` (parceiro **Rafael Ferreira Dias**, que é também o nome do CONSULTOR), `Erica`, `abel`, `melquiades`, `Daniel`. Prenome não identifica ninguém: “o Rafael falou comigo” dava o lead ao parceiro errado, e a keyword do parceiro competia com o nome do próprio dono do número. Pior, `deriveEffectiveKeywords` **adivinhava** qualificador pegando a palavra vizinha na frase do QR (“Zap” → “loja zap”) — palpite, não identidade. Corrigido em `_shared/keyword-matcher.ts`: (a) keyword que é pedaço do nome vira o **nome inteiro** quando ele aparece na frase do QR (`isPartOfPartnerName`); (b) a **frase do QR inteira** entra como chave extra (sequência longa e exata) e substitui a dedução por vizinho; (c) `matchKeyword` devolve **null** em empate de tamanho entre parceiros diferentes — empate é sorteio; (d) `isWeakNameKeyword` marca parceiro salvo só com prenome (“Daniel”, “Bruna”), e o `PartnerForm` **bloqueia** salvar assim — o sistema não inventa sobrenome. Espelho no front (`parceiros/qrPhrase.ts`) travado por `__tests__/keywordBlocklistParity.test.ts`.
62. **Rodapé do flyer com o telefone cru de `consultants.phone`** → `formatFlyerPhoneDisplay` só fatiava dígitos, então o celular gravado sem o nono dígito (`553484314317`) era **impresso** como “+55 (34) 8431-4317” — número inexistente — enquanto o QR do mesmo papel apontava para `5534984314317` (o `qr-redirect` normaliza). Papel impresso não tem rollback. Agora o front delega a `formatBrazilPhone` (`src/lib/phone.ts`) e o RPC `get_partner_banner_portal` devolve o número via `public.normalize_br_wa_phone` (migration `20260805213000`). Travas: `src/components/admin/__tests__/flyerPhoneDisplay.test.ts`.
63. **Handoff humano é pausa SEM prazo — lead morre entre o robô e o consultor** → o webhook (Whapi/Evolution) grava `paused_reason='handoff_humano'` + `next_action_at=NULL`. O `cadence-tick` até tinha expiração de 48h (`blockedCustomers`), mas ela só roda em linha **selecionada pelo claim**, e o claim exige `next_action_at <= now`: com NULL a linha nunca mais é vista e a expiração **jamais** acontece. Caso Robinho (lead do parceiro José, 2026-08-05): chegou pelo QR 15:46, o consultor mandou 2 áudios às 16:46, ninguém mais falou — fora do robô e fora do humano, e no portal do parceiro isso virava “0 pessoas no ciclo”. Correção: `_shared/bot/handoff-resume.ts` (`handoffResumeAtIso` agenda a reavaliação; `decideHandoffResume` decide na data; `HANDOFF_RELEASE_PATCH` libera o customer). Detalhes que não podem ser perdidos: a régua é a **última mensagem em `conversations`** (não `customers.updated_at`, que qualquer rotina empurra); soltar só o stage não basta porque `customers.bot_paused` barra o dispatch depois; handoff **não muda o stage**, então o tratamento vale para qualquer stage (não só `PAUSED`); `requested`/`opt_out`/`complaint`/`blocked`/`bulk_pro` e `do_not_contact` nunca voltam. O tick ainda varre linhas com `next_action_at IS NULL` como rede para qualquer caminho que zere a data. Testes: `_shared/bot/handoff-resume_test.ts`.
64. **Contar leitura de QR sem filtrar quem não é gente** → o portal do José mostrava 9 leituras para 2 conversas. Três fontes de ruído: `qr-redirect?json=1` (diagnóstico do admin) contava igual a cliente, prefetch/preview de link (WhatsApp, buscadores) também, e a mesma abertura chegava 2× com 1s de diferença. Agora o `qr-redirect` ignora `json=1` e user-agent de bot, grava `device_type` e descarta repetição do mesmo `event_target` em 15s; o RPC `get_partner_banner_portal` aplica a **mesma** janela de 15s por `lag()` no histórico já gravado (migration `20260805220000`) — sem apagar `page_events`.
65. **Lead do parceiro fora da pizza sem explicação** → “LEADS 2 / 0 pessoas no ciclo” destruía a confiança. Fechado / bloqueado / CRM / carteira ficam em `outside_cycle` com motivo. **Handoff humano NÃO fica de fora**: entra na pizza A (Ativo), com aviso “falando com o consultor” — pausado ≠ sumiu (migration `20260805223000`). Ao mexer em `cycle_leads`, mexa no `CASE` de `outside_cycle` junto.
