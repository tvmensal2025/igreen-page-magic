---
description: Hardening Ads — SQL JÁ APLICADO em produção (2026-07-25); edge functions deployadas
alwaysApply: true
---
# Hardening Ads: SQL aplicado

As migrations do hardening Ads **já foram aplicadas em produção** em 2026-07-25
(via MCP `apply_migration`), nesta ordem:

```
20260725170000_ads_orphan_crons_auth_headers.sql   (#0 órfãos)
20260724180000_ads_cron_auth_headers.sql
20260724190000_ads_spend_idempotent_billing.sql
20260724200000_ad_publish_saga.sql
20260724210000_facebook_capi_outbox.sql
```

Cadência P0/P1 também aplicada:
`20260724160000_cadence_inbound_preserve_bc` e
`20260724161000_crm_auto_progress_cron_auth`.

## Ainda NÃO ligar sem pedido explícito

- toggle `facebook_capi_dispatch` (está `false`)
- `ENFORCE_CRON_AUTH`
- expansão do Cérebro (`kill_switch=false` / `automation_mode` ≠ `disabled`)

## Pendência operacional restante

- `winner_photo_url` no `brain_config` do consultor piloto (Rafael): anchor já
  preenchido, mas sem foto estável HTTPS o rotator continua inerte
  (`anchor_campaign_not_configured` / foto ausente).
- Regenerar types: já feito em `src/integrations/supabase/types.ts`.

Detalhe: `docs/ads-hardening-rollout.md`.
