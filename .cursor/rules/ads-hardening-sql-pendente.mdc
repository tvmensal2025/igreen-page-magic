---
description: Hardening Ads + Cérebro que aprende (piloto full) — SQL aplicado
alwaysApply: true
---
# Hardening Ads: SQL aplicado + Cérebro que aprende

Migrations do hardening Ads **aplicadas** em 2026-07-25 (+ seed cron MG).

## Já ligado (piloto Rafael)

- `ENFORCE_CRON_AUTH=true`, `facebook_capi_dispatch=true`
- Cérebro `full`: rank→preferred_slugs, slots, seed_explorer (1/tick),
  creative_rotate (pausa ad loser), winner→`winner_photo_url`
- Proteções (waste/saldo) sempre on; `AUTO_PERF` só Play

## Ainda NÃO ligar sem pedido explícito

- `targeting_patch` automático
- `create_object` genérico no cron (use `seed_explorer`)
- expansão multi-consultor sem anchor + foto HTTPS

## Crons satélite (2026-07-25)

`assertCronAuth` + headers em lead-research / inbound-media-retry /
production-health / speed-to-lead / recover-stuck-otp (+ portal-offline,
flow-d-stuck, v3-rollout métricas). Jobs JWT-only desativados (não apagados):
classifier daily, meta-ads-metrics-3h, voice-dialer-health-30min.
Smoke: sem secret → 401; com `x-internal-secret` (settings) → 200.