{
  "name": "igreen-sync-worker",
  "version": "7.0.0",
  "description": "Playwright + Tor (IP residencial) — login via API REST do portal iGreen (sem captcha)",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs"
  },
  "dependencies": {
    "playwright-chromium": "^1.49.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
