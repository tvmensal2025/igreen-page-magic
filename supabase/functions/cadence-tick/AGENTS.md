# AGENTS — cadence-tick

Motor Zero Lead Perdido (~5 min). Domínio: `cadence` em `.kiro/steering/mapa-dominios.json`.

## Antes
1. `#fluxos` + `#helpers-canonicos` + caps em `regras-duras`
2. Evidência caps prod: B150 / C50 / G200 · janela reheat 08–20 (`#evidencia-prod`)

## Gates (ordem típica)
kill `bot_global` · toggle `cadence_engine` · DNC · janela BRT · cap A/B/C/global · cross-channel morto (IK/UNDELIV) · nome seguro

## A/B/C
`stageGroup()` em `_shared/cadence-engine.ts` — **A não conta** no global.

## NÃO
Descartar lead no cap (adia) · quiet hours em agenda humana · misturar com pós-venda
